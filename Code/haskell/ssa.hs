

-- propensity:: List -> (Integer, Float)


nextprop:: Int -> Float -> Int
nextprop x y =
    | aj =< r₂ * a₀, nextprop (suc(x))
    | else,



suc:: Int -> Int -- Recursive way to increment
suc j = j + 1
